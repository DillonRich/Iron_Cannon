{
  "refId": "cloudflare/d1-bindings",
  "sourceUrl": "https://developers.cloudflare.com/d1/worker-api/",
  "lastVerified": "2026-05-31",
  "title": "D1 Workers binding API",
  "excerpt": "Access via env.DB.prepare('SELECT...').bind().run()/.first()/.all(). Use batch() for transactions. Parameterized queries only—never string concat SQL. Return errors to client as generic messages; log details server-side.",
  "appliesTo": ["layer2/cloudflare", "owasp/sql-injection"],
  "tags": ["cloudflare", "d1", "bindings"]
}
